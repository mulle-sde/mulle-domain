### 1.6.2

* fix `local` variable usage in `r_filter_semver_tags_and_commits` loop for better shell compatibility
* fix completion script `local` usage
* add `.roam/` to gitignore

### 1.6.1

Various small improvements